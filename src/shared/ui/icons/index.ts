/**
 * Lucide icon proxy barrel export.
 * Always import icons from here — never directly from lucide-react.
 * This allows swapping icon libraries in one place if needed.
 */
export * from 'lucide-react'
